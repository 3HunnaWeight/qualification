/* import socketState from "./socketState";
const socket = new WebSocket(`ws://localhost:5000/`)
if(socketState.connection!==false){
    socketState.setSocket(socket)
    console.log(socket)
 
}
   

export default socket; */